version: 2

models:
  - name: stg_stripe__payments
    description: "{{ doc('stripe_payments') }}"
    columns:
      - name: payment_id
        description: primary key
        tests:
          - unique
          - not_null