version: 2

models:
  - name: stg_stripe__payments
    description: "{{ doc('stripe_payment') }}"
    columns:
      - name: payment_id
        description: primary key
        tests:
          - unique
          - not_null